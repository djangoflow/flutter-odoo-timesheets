import 'package:timesheets/configurations/configurations.dart';

@RoutePage(
  name: 'TimesheetsRouter',
)
class TimesheetsRouterPage extends AutoRouter {
  const TimesheetsRouterPage({super.key});
}
